// const addTwo = (num1, num2) => {
//     return num1 + num2   // Explicite return
// }      // But inside curly braces we have to use return keyword
  
// //console.log(addTwo(1,2))
// //const addOne = (num1, num2) => (num1 + num2) // Implicite return

// //const addOne = (num1, num2) => {username: "Amit"} // We cannot return object like this
// const addOne = (num1, num2) => ({username: "Amit"}) // We have to put it inside the parenthisis

// console.log(addOne())   // inside paranthisis we do not have to use return keyword 

